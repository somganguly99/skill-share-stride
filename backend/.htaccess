# Enable rewrite engine
RewriteEngine On

# Allow CORS preflight
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [R=200,L]

# Set error documents
ErrorDocument 404 /edulearn-api/404.php
ErrorDocument 500 /edulearn-api/500.php
